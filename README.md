# Salesforce CLI Workbench Versions

This repository is used only as a public remote manifest for desktop app version checks.

The app reads [`versions.json`](./versions.json) to determine:

- bundled Salesforce CLI version
- bundled `@pdfbutler/migration-cli` version
- minimum supported app version
- whether a forced update is required
